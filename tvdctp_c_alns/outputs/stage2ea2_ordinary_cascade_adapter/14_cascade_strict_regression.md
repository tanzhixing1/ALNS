# Cascade strict regression

The native Stage 2D contract, Cascade dependency closure, bundle partition,
dependency order, snapshot fields, validator semantics and Ω(B) implementation
were not changed. The adapted validator branch is selected only when the
ephemeral contract declares a trusted ordinary source and adapter version 1.

The complete A.1 + Stage 2D focused group passes, including native candidate
sequence, cross-van recovery, carrier feasibility, multi-drone coverage,
partial checker strictness, exact objective selection and atomic rollback.
