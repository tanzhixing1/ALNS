# Existing 13-pair strict regression

All 13 previously compatible pairs retain their A.1 values for removed IDs,
actual IDs, order, RNG, candidate traces/counts, selected result, objective,
business fingerprint, feasibility and violations.

The A.1 exact matrix test was narrowed only to these 13 pairs because A.2 now
owns the changed contract expectation for the former three blocked pairs.
Local/Regret trace digests and all 13 exact objective/fingerprint baselines
remain unchanged. Adapter call count is zero for all Global/Local/Regret pairs
and native Cascade+Cascade.
