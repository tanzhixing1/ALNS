# Default entry-point audit

- `main.py`: paper CLI default.
- `build_config()`: paper parameter default.
- `ALNSConfig`: paper field default.
- `run_c_alns()`: legacy missing field resolves to paper.
- `diagnose_calns.py`: paper diagnostic explicitly paper; current/engineering
  diagnostic explicitly extended.
- Tests and fixtures: default config is paper; no hidden full-dictionary read in
  the solver.
- Benchmark, separate experiment, separate multiseed/performance, and RL/PPO
  entry points: not present.

Unresolved entry points: none.
