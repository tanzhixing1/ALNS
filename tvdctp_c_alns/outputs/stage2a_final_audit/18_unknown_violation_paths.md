# Unknown Violation Paths

- Record 1: `cascade_aware_removal` → `best_mode_repair → _all_moves → _best_van_move/_best_drone_move → _apply_move → _finalize_repair` → `objective(candidate)` → `compute_timing` → `_record_customer_service:167-170` → `check_solution_feasible:1439-1441` → rejected (`accepted=False`) → current fingerprint restored.
- Record 2: `switch_transshipment_operator` → `regret_repair → _all_moves → _apply_move → _finalize_repair` → `objective(candidate)` → `compute_timing` → `_record_customer_service:167-170` → `check_solution_feasible:1439-1441` → rejected (`accepted=False`) → current fingerprint restored.
- Record 3: `route_segment_removal` → `greedy_drone_repair → _best_drone_move → _drone_insert_hard_feasible → _apply_move → optional greedy_van_repair → _finalize_repair` → `objective(candidate)` → `compute_timing` → `_record_customer_service:167-170` → `check_solution_feasible:1439-1441` → rejected (`accepted=False`) → current fingerprint restored.
- Record 4: `route_segment_removal` → `greedy_van_repair → _best_van_move → _van_insert_hard_feasible → _apply_move → _finalize_repair` → `objective(candidate)` → `compute_timing` → `_record_customer_service:167-170` → `check_solution_feasible:1439-1441` → rejected (`accepted=False`) → current fingerprint restored.
