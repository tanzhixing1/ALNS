# Invalid Mode Fail-Fast Check

**NOT RUN.**

Validation stopped after the paper 80-iteration main-entry timeout, following the prompt's stop-on-problem rule. No fallback claim is made from runtime evidence in this post-implementation run. The earlier static inspection confirms argparse choices are exactly `paper_mode` and `extended_mode`, but that is not substituted for the requested runtime command.
